on:
  push:
    # Sequence of patterns matched against refs/tags
    tags:
      - 'v*' # Push events to matching v*, i.e. v1.0, v20.15.10

name: Create Release

jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rid: [win-x64, linux-arm]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 3.1.301
      - name: Restore dependencies
        run: dotnet restore -r ${{ matrix.rid }} src/HouseStock.Api/HouseStock.Api.csproj
      - name: Build
        run: dotnet build -c Release -r ${{ matrix.rid }} src/HouseStock.Api/HouseStock.Api.csproj
      - name: Publish
        run: dotnet publish -c Release -r ${{ matrix.rid }} -p:PublishSingleFile=true --no-restore --no-build -o publish/ src/HouseStock.Api/HouseStock.Api.csproj
      - name: Create archive
        run: zip -r -j HouseStock-${{ github.ref }}-${{ matrix.rid }}.zip publish/*
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        with:
          tag_name: ${{ github.ref }}
          release_name: Release-${{ github.ref }}-${{ matrix.rid }}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset 
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} 
          asset_path: ./HouseStock-${{ github.ref }}-${{ matrix.rid }}.zip
          asset_name: HouseStock-${{ github.ref }}-${{ matrix.rid }}.zip
          asset_content_type: application/zip
